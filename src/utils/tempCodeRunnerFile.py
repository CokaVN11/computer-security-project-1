>", lambda event, fp_label=file_path_label, ft="KPrivate File": self.browse_file(fp_label, ft))

        # # button to verify KPrivate
        # self.verify_button = self.create_custom_button("Decryption", "VERIFY KPrivate", self.export_Kprivate, 0, 4)
        # self.verify_button.configure(width=5)
        # self.create_separator("Decryption", 1, 0, 5)